Создать проект npm create vite@latest my-frontend -- --template react

Запуск npm run dev  

Роутеры npm install react-router-dom

Библиотека для музыки npm install tone
npm install axios


Установить бэкенд ларавель composer create-project laravel/laravel backend

Перенести сист таблицы из бд в бэк php artisan migrate

Шаг 1.1: Установи Sanctum
composer require laravel/sanctum
php artisan vendor:publish --provider="Laravel\Sanctum\SanctumServiceProvider"
php artisan migrate



создание контроллеров php artisan make:controller Api/AuthController
символическая ссылка для хранения файлов php artisan storage:link

http://127.0.0.1:5173/
http://127.0.0.1:8000/api/users


npm run dev -- --host 127.0.0.1

php artisan config:clear
php artisan route:clear
php artisan cache:clear
php artisan optimize:clear
php artisan serve --host=127.0.0.1 --port=8000           





миграции.............

php artisan make:migration update_users_table_constraints_and_lengths --table=users      

я так пон на компе я подключ к локальной бд
из пгадмина

а на ноуте ТОЖЕ но пароль от постгрескл postgres
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=synethia
DB_USERNAME=postgres
DB_PASSWORD=postgres

php artisan migrate


докер всё, не использую уже 
docker compose down -v
docker compose build --no-cache
docker compose up -d

docker compose up -d database  

docker compose exec backend php artisan migrate

